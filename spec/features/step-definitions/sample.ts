// tslint:disable:no-console
import { Given, Then, When } from "cucumber";

Given("context", () => {
  // Do nothing
});

When("event", () => {
  // Do nothing
});

Then("outcome", () => {
  // Do nothing
});
